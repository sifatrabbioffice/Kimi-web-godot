name: 🎮 Build Godot Web Editor from Source → Unsigned IPA

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

env:
  GODOT_BRANCH: "4.6-stable"
  EMSDK_VERSION: "4.0"

jobs:
  # ═══════════════════════════════════════════════════════════════
  # JOB 1: Compile Godot Web Editor from Source
  # ═══════════════════════════════════════════════════════════════
  compile-godot:
    runs-on: macos-latest
    steps:
      - name: 📥 Checkout User Repo
        uses: actions/checkout@v4

      # ── Cache Emscripten ──
      - name: 💾 Cache Emscripten
        uses: actions/cache@v4
        id: emsdk-cache
        with:
          path: emsdk
          key: emsdk-${{ env.EMSDK_VERSION }}-${{ runner.os }}

      # ── Install Emscripten ──
      - name: 🔧 Install Emscripten
        run: |
          set -e
          if [ ! -d "emsdk" ]; then
            echo "Cloning Emscripten SDK..."
            git clone --depth 1 https://github.com/emscripten-core/emsdk.git
          fi
          cd emsdk
          ./emsdk install ${{ env.EMSDK_VERSION }}
          ./emsdk activate ${{ env.EMSDK_VERSION }}

      # ── Cache compiled Godot ──
      - name: 💾 Cache Compiled Godot
        uses: actions/cache@v4
        id: godot-cache
        with:
          path: godot_editor
          key: godot-web-editor-${{ env.GODOT_BRANCH }}-${{ hashFiles('emsdk/**/*') }}

      # ── Install Build Tools ──
      - name: 🛠️ Install SCons
        if: steps.godot-cache.outputs.cache-hit != 'true'
        run: |
          pip3 install scons --break-system-packages 2>/dev/null ||           pip3 install scons --user 2>/dev/null ||           brew install scons

      # ── Clone Godot Source ──
      - name: 📦 Clone Godot Source
        if: steps.godot-cache.outputs.cache-hit != 'true'
        run: |
          set -e
          echo "Cloning Godot ${{ env.GODOT_BRANCH }}..."
          git clone --depth 1 --branch ${{ env.GODOT_BRANCH }}             https://github.com/godotengine/godot.git godot-src

      # ── Compile Godot Web Editor ──
      - name: 🔨 Compile Godot Web Editor
        if: steps.godot-cache.outputs.cache-hit != 'true'
        run: |
          set -e
          source emsdk/emsdk_env.sh

          echo "Starting Godot Web Editor compilation..."
          echo "This may take 20-40 minutes. Please wait."

          cd godot-src

          # Build web editor with 4 parallel jobs
          scons platform=web target=editor production=yes             -j$(sysctl -n hw.ncpu)             2>&1 | tee ../build.log

          echo "✅ Compilation complete!"

      # ── Collect Output Files ──
      - name: 📂 Collect Built Files
        if: steps.godot-cache.outputs.cache-hit != 'true'
        run: |
          set -e
          mkdir -p godot_editor

          echo "Searching for compiled web editor files..."

          # Godot outputs to bin/ directory
          SRC="godot-src/bin"

          if [ -d "$SRC" ]; then
            echo "Files in $SRC:"
            ls -la "$SRC" || true

            # Copy all web-related files
            cp -v "$SRC"/godot.*.html godot_editor/index.html 2>/dev/null ||             cp -v "$SRC"/*.html godot_editor/index.html 2>/dev/null || true

            cp -v "$SRC"/godot.editor.* godot_editor/ 2>/dev/null || true
            cp -v "$SRC"/godot.web.* godot_editor/ 2>/dev/null || true
            cp -v "$SRC"/*.wasm godot_editor/ 2>/dev/null || true
            cp -v "$SRC"/*.js godot_editor/ 2>/dev/null || true
            cp -v "$SRC"/*.data godot_editor/ 2>/dev/null || true
            cp -v "$SRC"/*.zip godot_editor/ 2>/dev/null || true
          fi

          # Also check misc/dist/html directory for prebuilt editor files
          if [ -d "godot-src/misc/dist/html" ]; then
            cp -v godot-src/misc/dist/html/* godot_editor/ 2>/dev/null || true
          fi

          # Rename main HTML to index.html if needed
          cd godot_editor
          for f in godot.editor.html godot.web.editor.html godot.html; do
            if [ -f "$f" ] && [ ! -f index.html ]; then
              mv "$f" index.html
              echo "Renamed $f → index.html"
              break
            fi
          done

          echo "📦 Final files in godot_editor:"
          ls -lah

      # ── Upload compiled files as artifact ──
      - name: 📤 Upload Godot Editor Files
        uses: actions/upload-artifact@v4
        with:
          name: godot-web-editor-files
          path: godot_editor/
          retention-days: 30

  # ═══════════════════════════════════════════════════════════════
  # JOB 2: Build iOS IPA (depends on compile job)
  # ═══════════════════════════════════════════════════════════════
  build-ipa:
    runs-on: macos-latest
    needs: compile-godot
    steps:
      - name: 📥 Checkout User Repo
        uses: actions/checkout@v4

      - name: 🔧 Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - name: 🍺 Install xcodegen
        run: brew install xcodegen

      # ── Download compiled Godot files ──
      - name: ⬇️ Download Godot Editor Files
        uses: actions/download-artifact@v4
        with:
          name: godot-web-editor-files
          path: godot_editor/

      # ── Create iOS App ──
      - name: 🏗️ Create iOS Wrapper App
        run: |
          set -e

          # Verify files exist
          if [ ! -f godot_editor/index.html ]; then
            echo "❌ index.html not found in godot_editor/"
            ls -la godot_editor/
            exit 1
          fi

          echo "✅ Godot files verified"
          ls -la godot_editor/

          # Create directories
          mkdir -p GodotWebEditor/Sources
          mkdir -p GodotWebEditor/Resources/godot_editor
          mkdir -p GodotWebEditor/Resources/Base.lproj
          mkdir -p GodotWebEditor/Resources/Assets.xcassets/AppIcon.appiconset

          # Copy all Godot files into app bundle
          cp -R godot_editor/* GodotWebEditor/Resources/godot_editor/

          # ── SwiftUI App ──
          cat > GodotWebEditor/Sources/GodotWebEditorApp.swift << 'EOF'
          import SwiftUI
          import WebKit

          @main
          struct GodotWebEditorApp: App {
              var body: some Scene {
                  WindowGroup {
                      GodotEditorView()
                          .edgesIgnoringSafeArea(.all)
                  }
              }
          }

          struct GodotEditorView: UIViewRepresentable {
              func makeUIView(context: Context) -> WKWebView {
                  let config = WKWebViewConfiguration()
                  config.preferences.javaScriptEnabled = true

                  if #available(iOS 15.0, *) {
                      let prefs = WKWebpagePreferences()
                      prefs.allowsContentJavaScript = true
                      config.defaultWebpagePreferences = prefs
                  }

                  let webView = WKWebView(frame: .zero, configuration: config)
                  webView.scrollView.bounces = false
                  webView.backgroundColor = .black
                  webView.isOpaque = true

                  if let url = Bundle.main.url(
                      forResource: "index",
                      withExtension: "html",
                      subdirectory: "godot_editor"
                  ) {
                      let base = url.deletingLastPathComponent()
                      webView.loadFileURL(url, allowingReadAccessTo: base)
                  }
                  return webView
              }

              func updateUIView(_ uiView: WKWebView, context: Context) {}
          }
          EOF

          # ── Info.plist ──
          cat > GodotWebEditor/Info.plist << 'EOF'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>CFBundleDevelopmentRegion</key><string>en</string>
              <key>CFBundleDisplayName</key><string>Godot Editor</string>
              <key>CFBundleExecutable</key><string>GodotWebEditor</string>
              <key>CFBundleIdentifier</key><string>com.godot.webeditor.local</string>
              <key>CFBundleInfoDictionaryVersion</key><string>6.0</string>
              <key>CFBundleName</key><string>GodotWebEditor</string>
              <key>CFBundlePackageType</key><string>APPL</string>
              <key>CFBundleShortVersionString</key><string>1.0.0</string>
              <key>CFBundleVersion</key><string>1</string>
              <key>LSRequiresIPhoneOS</key><true/>
              <key>UILaunchStoryboardName</key><string>LaunchScreen</string>
              <key>UIRequiredDeviceCapabilities</key><array><string>arm64</string></array>
              <key>UISupportedInterfaceOrientations</key>
              <array>
                  <string>UIInterfaceOrientationPortrait</string>
                  <string>UIInterfaceOrientationLandscapeLeft</string>
                  <string>UIInterfaceOrientationLandscapeRight</string>
              </array>
              <key>UISupportedInterfaceOrientations~ipad</key>
              <array>
                  <string>UIInterfaceOrientationPortrait</string>
                  <string>UIInterfaceOrientationPortraitUpsideDown</string>
                  <string>UIInterfaceOrientationLandscapeLeft</string>
                  <string>UIInterfaceOrientationLandscapeRight</string>
              </array>
              <key>UIViewControllerBasedStatusBarAppearance</key><true/>
              <key>UIStatusBarHidden</key><true/>
          </dict>
          </plist>
          EOF

          # ── LaunchScreen.storyboard ──
          cat > GodotWebEditor/Resources/Base.lproj/LaunchScreen.storyboard << 'EOF'
          <?xml version="1.0" encoding="UTF-8"?>
          <document type="com.apple.InterfaceBuilder3.CocoaTouch.Storyboard.XIB" version="3.0" toolsVersion="21701" targetRuntime="iOS.CocoaTouch" propertyAccessControl="none" useAutolayout="YES" launchScreen="YES" useTraitCollections="YES" useSafeAreas="YES" colorMatched="YES" initialViewController="01J-lp-oVM">
              <device id="retina6_12" orientation="portrait" appearance="dark"/>
              <dependencies>
                  <deployment identifier="iOS"/>
                  <plugIn identifier="com.apple.InterfaceBuilder.IBCocoaTouchPlugin" version="21679"/>
              </dependencies>
              <scenes>
                  <scene sceneID="EHf-IW-A2E">
                      <objects>
                          <viewController id="01J-lp-oVM" sceneMemberID="viewController">
                              <view key="view" contentMode="scaleToFill" id="Ze5-6b-2t3">
                                  <rect key="frame" x="0.0" y="0.0" width="393" height="852"/>
                                  <autoresizingMask key="autoresizingMask" widthSizable="YES" heightSizable="YES"/>
                                  <subviews>
                                      <label opaque="NO" clipsSubviews="YES" userInteractionEnabled="NO" contentMode="left" horizontalHuggingPriority="251" verticalHuggingPriority="251" text="Godot Web Editor" textAlignment="center" lineBreakMode="middleTruncation" baselineAdjustment="alignBaselines" minimumFontSize="18" translatesAutoresizingMaskIntoConstraints="NO" id="GJd-Yh-RWb">
                                          <rect key="frame" x="0.0" y="263.66666666666669" width="393" height="43"/>
                                          <fontDescription key="fontDescription" type="boldSystem" pointSize="36"/>
                                          <color key="textColor" red="0.93725490570000003" green="0.93725490570000003" blue="0.95686274770000002" alpha="1" colorSpace="custom" customColorSpace="sRGB"/>
                                          <nil key="highlightedColor"/>
                                      </label>
                                      <label opaque="NO" clipsSubviews="YES" userInteractionEnabled="NO" contentMode="left" horizontalHuggingPriority="251" verticalHuggingPriority="251" text="Offline • Compiled from Source • Metal GPU" textAlignment="center" lineBreakMode="tailTruncation" baselineAdjustment="alignBaselines" adjustsFontSizeToFit="YES" translatesAutoresizingMaskIntoConstraints="NO" id="MN2-Qa-3nb">
                                          <rect key="frame" x="0.0" y="314.66666666666669" width="393" height="21"/>
                                          <fontDescription key="fontDescription" type="system" pointSize="15"/>
                                          <color key="textColor" red="0.5803921569" green="0.5960784314" blue="0.6392156863" alpha="1" colorSpace="custom" customColorSpace="sRGB"/>
                                          <nil key="highlightedColor"/>
                                      </label>
                                  </subviews>
                                  <color key="backgroundColor" red="0.05098039216" green="0.06666666667" blue="0.09019607843" alpha="1" colorSpace="custom" customColorSpace="sRGB"/>
                                  <constraints>
                                      <constraint firstItem="GJd-Yh-RWb" firstAttribute="centerX" secondItem="Ze5-6b-2t3" secondAttribute="centerX" id="e1q-4Y-1Sd"/>
                                      <constraint firstItem="GJd-Yh-RWb" firstAttribute="centerY" secondItem="Ze5-6b-2t3" secondAttribute="centerY" constant="-20" id="Rmq-lb-GrQ"/>
                                      <constraint firstItem="MN2-Qa-3nb" firstAttribute="top" secondItem="GJd-Yh-RWb" secondAttribute="bottom" constant="8" id="abc-12-xyz"/>
                                      <constraint firstItem="MN2-Qa-3nb" firstAttribute="centerX" secondItem="Ze5-6b-2t3" secondAttribute="centerX" id="def-34-uvw"/>
                                  </constraints>
                              </view>
                          </viewController>
                          <placeholder placeholderIdentifier="IBFirstResponder" id="iYj-Kq-Ea1" userLabel="First Responder" sceneMemberID="firstResponder"/>
                      </objects>
                      <point key="canvasLocation" x="53" y="375"/>
                  </scene>
              </scenes>
          </document>
          EOF

          # ── Assets ──
          echo '{"info":{"author":"xcode","version":1}}' > GodotWebEditor/Resources/Assets.xcassets/Contents.json
          echo '{"images":[{"filename":"appicon.png","idiom":"universal","platform":"ios","size":"1024x1024"}],"info":{"author":"xcode","version":1}}' > GodotWebEditor/Resources/Assets.xcassets/AppIcon.appiconset/Contents.json

          # ── App Icon (pure Python, no Pillow) ──
          python3 << 'PYEOF'
          import struct, zlib
          w, h = 1024, 1024
          raw = bytes([0x47, 0x8C, 0xB0] * (w * h))
          def png_chunk(t, d):
              c = struct.pack(">I", len(d)) + t + d
              return c + struct.pack(">I", zlib.crc32(t + d) & 0xffffffff)
          ihdr = struct.pack(">IIBBBBB", w, h, 8, 2, 0, 0, 0)
          idat = zlib.compress(b''.join([b'\x00' + raw[i*w*3:(i+1)*w*3] for i in range(h)]))
          png = b'\x89PNG\r\n\x1a\n' + png_chunk(b'IHDR', ihdr) + png_chunk(b'IDAT', idat) + png_chunk(b'IEND', b'')
          with open('GodotWebEditor/Resources/Assets.xcassets/AppIcon.appiconset/appicon.png', 'wb') as f:
              f.write(png)
          print("✅ Icon created")
          PYEOF

          # ── xcodegen project ──
          cat > GodotWebEditor/project.yml << 'EOF'
          name: GodotWebEditor
          settings:
            MARKETING_VERSION: "1.0.0"
            CURRENT_PROJECT_VERSION: "1"
            PRODUCT_BUNDLE_IDENTIFIER: com.godot.webeditor.local
            SWIFT_VERSION: "5.9"
            IPHONEOS_DEPLOYMENT_TARGET: "15.0"
            TARGETED_DEVICE_FAMILY: "1,2"
            CODE_SIGN_IDENTITY: ""
            CODE_SIGNING_REQUIRED: NO
            CODE_SIGNING_ALLOWED: NO
            DEVELOPMENT_TEAM: ""
            ENABLE_BITCODE: NO
          targets:
            GodotWebEditor:
              type: application
              platform: iOS
              deploymentTarget: "15.0"
              sources:
                - path: Sources
              resources:
                - path: Resources
              info:
                path: Info.plist
          EOF

          cd GodotWebEditor
          xcodegen generate
          cd ..

      # ── Build Unsigned .app ──
      - name: 🔨 Build .app
        run: |
          set -e
          cd GodotWebEditor

          xcodebuild -project GodotWebEditor.xcodeproj             -scheme GodotWebEditor             -configuration Release             -destination 'generic/platform=iOS'             -derivedDataPath ../DerivedData             CODE_SIGN_IDENTITY=""             CODE_SIGNING_REQUIRED=NO             CODE_SIGNING_ALLOWED=NO             DEVELOPMENT_TEAM=""             PROVISIONING_PROFILE_SPECIFIER=""             ONLY_ACTIVE_ARCH=NO             ARCHS="arm64"             ENABLE_BITCODE=NO             clean build

          cd ..

      # ── Package IPA ──
      - name: 📦 Package IPA
        run: |
          set -e

          APP_PATH=$(find DerivedData -name "GodotWebEditor.app" -type d | head -n 1)

          if [ -z "$APP_PATH" ] || [ ! -d "$APP_PATH" ]; then
            echo "❌ .app bundle not found"
            find DerivedData -type d -name "*.app" || true
            exit 1
          fi

          echo "✅ Found: $APP_PATH"

          mkdir -p Payload
          cp -R "$APP_PATH" Payload/
          rm -rf Payload/GodotWebEditor.app/_CodeSignature 2>/dev/null || true

          IPA_NAME="GodotWebEditor_v4.6_source_unsigned.ipa"
          zip -qr "$IPA_NAME" Payload
          rm -rf Payload

          echo "IPA_NAME=$IPA_NAME" >> $GITHUB_ENV
          echo "✅ Created: $IPA_NAME"
          ls -lh "$IPA_NAME"

      # ── Upload IPA ──
      - name: 📤 Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: godot-web-editor-ipa
          path: GodotWebEditor_v4.6_source_unsigned.ipa
          retention-days: 30

      - name: ✅ Done
        run: |
          echo ""
          echo "╔══════════════════════════════════════════════════════╗"
          echo "║  🎉 GODOT WEB EDITOR IPA BUILD COMPLETE              ║"
          echo "╠══════════════════════════════════════════════════════╣"
          echo "║  📁 File: ${{ env.IPA_NAME }}                        ║"
          echo "║  📱 Install: AltStore / Sideloadly / Scarlet         ║"
          echo "║  🔧 Source: Compiled from Godot ${{ env.GODOT_BRANCH }} ║"
          echo "╚══════════════════════════════════════════════════════╝"
